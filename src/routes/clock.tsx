'use client';

import { POPULAR_TIMEZONES } from '@/lib/timezone';
import { DigitalClockDashboard } from '@/components/DigitalClockDashboard';

export function ClockPage() {
  return (
    <div>
      <DigitalClockDashboard />
    </div>
  );
}
